export type PropTypes = {
  data: {
    sanityLab: {
      id: string;
      subline: string;
      title: string;
      _rawContent: Array<{
        _key: string;
        _type: string;
        [key: string]: any;
      }>;
    };
  };
};
